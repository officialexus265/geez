# Required Vercel env

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

Get it from: Supabase → Project Settings → API → service_role (secret)

Without this key, deposits stay "pending" and never show on the dashboard.
